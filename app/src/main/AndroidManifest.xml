<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskSpehere">
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <!-- SPLASH (LAUNCHER) -->

        <activity
            android:name=".view.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- AUTH -->
        <activity
            android:name=".view.auth.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".view.auth.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".view.auth.ForgotPasswordActivity"
            android:exported="true" /> <!-- DASHBOARD -->
        <activity
            android:name=".view.dashboard.DashboardActivity"
            android:exported="true" /> <!-- ITEM SCREENS -->
        <activity
            android:name=".view.dashboard.AddItemActivity"
            android:exported="true" />
        <activity
            android:name=".view.dashboard.EditItemActivity"
            android:exported="true" />
        <activity
            android:name=".view.dashboard.ViewItemActivity"
            android:exported="true" />
    </application>

</manifest>